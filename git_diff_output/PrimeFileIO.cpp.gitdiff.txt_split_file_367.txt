chatGPTInstruction:317+
+bool PrimeFileIO::CheckNextChar(FILE *f, char c)
+{
+    int i;
+    while ((i = getc(f)) != EOF)
+    {
+        if ((char)i == ' ' || (char)i == '.' || (char)i == '\n')
+            continue;
+        ungetc((char)i, f);
+        return ((char)i == c);
+    }
+    return false;
+}
