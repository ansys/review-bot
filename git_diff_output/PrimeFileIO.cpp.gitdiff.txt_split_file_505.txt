chatGPTInstruction:455+
+/* f format is (1 2 3 4)*/
+void PrimeFileIO::ReadList(FILE *f, char *token, std::vector<int> &list)
+{
+    if (!ReadNextTokenAndCheckStartList(f, token))
+        return;
+    ReadOpenedList(f, token, list);
+}
