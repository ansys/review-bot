chatGPTInstruction:414+
+/* f format is 1.0 2.0 0.3 0.4)*/
+void PrimeFileIO::ReadOpenedList(FILE *f, char *token, std::vector<double> &list)
+{
+    ReadNextToken(f, token);
+    while (token[0] != ')')
+    {
+        list.push_back((double)atof(token));
+        ReadNextToken(f, token);
+    }
+
+    return;
+}
