chatGPTInstruction:222+
+/*ReadString*/
+void PrimeFileIO::ReadString(FILE *f, char *token)
+{
+    /*TGEnv env = m_model->GetTGEnv();*/
+    int i;
+
+    while ((i = getc(f)) != EOF)
+    {
+        if ((char)i == '"')
+        {
+            *token = '\0';
+            return;
+        }
+        if ((char)i == '\\' && getc(f) == EOF)
+        {
+            break;
+        }
+        *token = (char)i;
+        token++;
+    }
+    // EOF_Error(env);
+    return;
+}
