chatGPTInstruction:359+
+/* move file pointer just past closing paren of current list */
+void PrimeFileIO::FlushReadList(FILE *f)
+{
+    // TGEnv env = m_model->GetTGEnv();
+    int i;
+
+    while ((i = getc(f)) != EOF)
+    {
+        if ((char)i == ')')
+            return;
+        else if ((char)i == '(')
+            FlushReadList(f);
+        else if ((char)i == '"')
+            FlushString(f);
+    }
+    // EOF_Error(env);
+    return;
+}
