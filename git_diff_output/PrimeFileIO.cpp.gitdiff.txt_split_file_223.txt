chatGPTInstruction:173+
+/*FlushString*/
+void PrimeFileIO::FlushString(FILE *f)
+{
+    int i;
+    /*TGEnv env = m_model->GetTGEnv();*/
+
+    while ((i = getc(f)) != EOF)
+    {
+        if ((char)i == '"')
+            return;
+        else if ((char)i == '\\')
+            if (getc(f) == EOF)
+                break;
+    }
+
+    return;
+}
